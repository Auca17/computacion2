services:
  monitor:
    build: .
    tty: true
    stdin_open: true
    pid: "host"
    privileged: true
    volumes:
      - /proc:/host_proc:ro
    environment:
      - PROC_BASE=/host_proc
      - TERM=xterm-256color
      - PYTHONDONTWRITEBYTECODE=1
